name: Go Security Scan

on:
  push:
    branches:
      - main
      - 'releases/**'
      - 'branch1'

jobs:
  security_scan:
    name: Security Scan
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Set up Go
        uses: actions/setup-go@v2
        with:
          go-version: '1.16' # Set the Go version you are using

      - name: Run Trivy vulnerability scanner
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: 'fs'
          ignore-unfixed: true
          severity: 'CRITICAL'
          format: 'table' # Output format (optional)

      - name: Build Docker Image
        run: docker build -t myorg/my-golang-app:${{ github.sha }} .
      
      - name: Push to Docker Hub
        if: success()
        env:
          DOCKER_USERNAME: poonamkhichade720
          DOCKER_PASSWORD: PoonamK123*
        run: |
          echo "${DOCKER_PASSWORD}" | docker login -u "${DOCKER_USERNAME}" --password-stdin
          docker push poonamkhichade720/ci-cdworkshop:${{ github.sha }}
